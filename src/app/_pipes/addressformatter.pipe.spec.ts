import { AddressformatterPipe } from './addressformatter.pipe';

describe('AddressformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
